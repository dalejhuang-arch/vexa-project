"use client";
import { ChevronDown, ChevronUp, Clock, CreditCard, Gift, KeyRound, ShieldAlert, UserX, AlertTriangle } from "lucide-react";
import { useState } from "react";
const items = [[Clock, "Urgency", "Compresses your time so verification feels impossible."], [ShieldAlert, "False authority", "Borrows a trusted institution's credibility."], [UserX, "Isolation", "Cuts off people who could challenge the story."], [AlertTriangle, "Threat", "Uses fear of punishment or loss to force action."], [Gift, "Too good to be true", "Offers an unusually attractive outcome to lower skepticism."], [CreditCard, "Payment request", "Pushes for money through an unusual or irreversible method."], [KeyRound, "Personal info", "Requests credentials, codes, or identity details."] ] as const;
export function TacticGlossary() { const [open, setOpen] = useState(false); return <div className="console-card p-5"><button onClick={() => setOpen(!open)} className="flex w-full items-center justify-between text-left" aria-expanded={open}><span className="eyebrow">TACTIC GLOSSARY</span>{open ? <ChevronUp size={16} /> : <ChevronDown size={16} />}</button>{open && <div className="mt-4 space-y-3">{items.map(([Icon, title, definition]) => <div key={title} className="flex gap-3 text-sm"><Icon size={16} className="mt-0.5 shrink-0 text-blue-500" /><p><strong>{title}</strong><span className="ml-2 text-[var(--muted)]">{definition}</span></p></div>)}</div>}</div>; }
